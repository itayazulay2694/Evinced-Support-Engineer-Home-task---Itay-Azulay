/**
 * @file Shared relative path for per-spec Evinced HTML reports (`evSaveFile`).
 * Must stay aligned with `report.outputDir` in `evConfig.json`.
 */
export const EVINCED_REPORT_DIR = './evinced-reports';
